include "application.conf"

play.cache.redis {
  # bind the instance to unqualified APIs
  bind-default: true
  # unqualified APIs bind to "local" instance
  default-cache: "local"

  instances {
    "local": {
      host: ${?REDIS_URL}
      port: 6379
      database: 1
      password: null
    }

    "remote": {
      host: ${?REDIS_URL}
      port: 6379
      database: 2
      password: null
    }
  }
}

PGDBIP: "192.168.100.75"
IMPALA_HOST: "192.168.100.51"
impala.port: "21050"